#!/bin/sh

# I almost always only use the interactive mode, so it makes sense to always use
# the '-i' option. This is a script and not an alias so that I don't need to
# open a terminal for this, but can simply call it using dmenu.

gnome-screenshot -i
